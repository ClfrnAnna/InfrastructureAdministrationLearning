Starting:

docker-compose -f docker-compose.yml -f docker-compose-infra.yml up -d --build

Testing for PowerShell:

$orderId = (Invoke-RestMethod -Uri "http://localhost:8003/order/create" -Method Post -Body "Test order" -ContentType "text/plain").order_id
Start-Sleep -Seconds 3
$order = Invoke-RestMethod -Uri "http://localhost:8003/order/$orderId"
$order | Format-List



docker network create app1-network
docker network create app2-network
docker-compose -f docker-compose-infra.yml up -d --build
docker-compose -f docker-compose-services.yml up -d --build

or

docker-compose -f docker-compose-infra.yml -f docker-compose-services.yml up -d


or

docker-compose -f docker-compose-infra.yml up -d
Start-Sleep -Seconds 15
docker-compose -f docker-compose-services.yml up -d


в docker-compose добавь, где не добавил volumes.
как пройти флоу тестового заказа: создать заказ через /order/create , получить статус заказа через
GET/order/<id>.
